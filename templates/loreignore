# .loreignore — paths whose sessions never generate lore.
# One glob per line. Blank lines and lines starting with # are ignored.
# Keep secrets and machine-generated code out of the shared brain.

secrets/**
.env
.env.*
**/*.pem
**/*.key
vendor/**
node_modules/**
dist/**
build/**
